package day20_Arrays;

import java.util.Arrays;

public class examples {
    public static void main(String[] args) {
        int[]numbers={10,7,8,64};
        System.out.println(Arrays.toString(numbers));
        Arrays.sort(numbers);
        System.out.println(Arrays.toString(numbers));
    }
}
